# Examples

The examples use an in-memory ring to isolate consumption semantics. Real
authorization and descriptor transfer are implemented by the reference bridge
and client. These programs teach the API; they are not benchmark workloads.
