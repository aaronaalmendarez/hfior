# Plots

- `physical-game-fps.png`: FPS comparison from the five-pair real-mouse test.
- `late-latch-p99.png`: slow-end input age in the generated-input test.
- `action-scaling-model.png`: predicted heavy game updates as mouse rate rises.

All plots are generated by [`plot_results.py`](../../tools/plots/plot_results.py).
The FPS plot reads the retained physical-test
[`aggregate.json`](../raw/physical-game-8k/aggregate.json) directly. Audit the
linked processed and raw data before citing a value.
