# Processed evidence

`hfior/` contains processed **synthetic** joined-trace and scaling outputs.
Field names retain nanosecond units where applicable. Derived plots live in
`../plots/`. The physical game aggregate remains beside its raw inputs under
`../raw/physical-game-8k/`.
